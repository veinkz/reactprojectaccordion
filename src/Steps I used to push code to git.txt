Open GitCMD
GO into the working folder
write command git init
then write git add .
then write git commit -m "messageforcommit"
and then git push gitrepo-url master

IMP - I had used main instead of master in the last command... but it gave me error
